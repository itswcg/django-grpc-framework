============
Installation
============

At the command line::

    $ easy_install django-grpc-framework

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-grpc-framework
    $ pip install django-grpc-framework
